// values
// return the array of values for each property in an object
const person = {
    name: 'Rey',
    age: 20,
    hobbies: ['reading', 'watching tv']
};
console.log(Object.values(person));

// entries
// returns an array of objects key and value pairs
console.log(Object.entries(person));